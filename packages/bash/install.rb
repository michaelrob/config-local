require 'fileutils.rb'

class Install

  # ToDo: add things related to bash installing

  # ToDo: install bash_config
    # install bashrc
    # install completion fileu

  # ToDo: change this to do some bash_config generation rather than copying my bash_config.
end
