{
  "name": "completion scripts",
  "description": "Completion scripts are a collection of scripts used in bash completion, to work entires must be installed in your bash_profile.",
  "commands": [
    "touch ~/scripts",
    "cp git-completion.bash ~/scripts/",
    "cp grails_completion ~/scripts/",
    "cp rake_completion ~/scripts/",
    "cp ssh_completion ~/scripts/",
    "cp thor_completion ~/scripts/",
    "cp list_apps_completion ~/scripts/"
  ]
}
